<%- include('../partials/header') %>

<main>
  <div class="faculty-detail">
    <img class="faculty-photo" src="/images/faculty/<%=faculty.slug%>.jpg" alt="Faculty photo">
    <div class="faculty-title">
      <h1><%= faculty.name %></h1>
      <p class="facultyId"><%= faculty.title %></p>
      <p class="department"><%= faculty.department %></p>
    </div>
  </div>

  <div class="sections">
    <div class="faculty-info">
      <p><strong>Office:</strong> <%= faculty.office %></p>
      <p><strong>Phone:</strong> <%= faculty.phone %></p>
      <p><strong>Email:</strong> <%= faculty.email %></a></p>
    </div>
  </div>

  <div class="navigation">
    <p><a href="/faculty">&larr; Back to Faculty Directory</a></p>
  </div>
</main>

<%- include('../partials/footer') %>